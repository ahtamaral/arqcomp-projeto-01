		# print a[i] (key)
		#li $v0, 1
		#move $a0, $t5
		#syscall
		
		# ---
		# Debug índices (i, j)
	    li $v0, 1
		move $a0, $t0
		syscall
		move $a0, $t1
		syscall
        move $a0, $t5
		syscall	
		 ---